/**
 * Datos de arranque (generados o estáticos) sin lectura de almacenamiento.
 * El merge con persistencia ocurre en el servicio de catálogo (`ClubCatalogPort`).
 */

export { buildClubDataDefaults } from '@/lib/clubDataDefaults';
